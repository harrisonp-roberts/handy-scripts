# Mimeify
Mimeify is a tool that detects the MIME type of each file in a directory, and appends it to the filename.

It is useful if you have many files of different types that don't have a file extension, but should have one.